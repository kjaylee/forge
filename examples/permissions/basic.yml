# Basic permission configuration
read:
  type: "Once"  # Ask for each read operation

write:
  type: "Once"  # Ask for each write operation

execute:
  type: "Always"
  whitelist:
    type: "Some"
    commands:
      - "ls"
      - "git status"
      - "git diff"
      - "git log"
      - "git show"